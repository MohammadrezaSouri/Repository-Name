# Repository-Name
A lightweight REST API server built from scratch in pure Java SE (no frameworks) using com.sun.net.httpserver — supports user registration, update, and deletion with PostgreSQL persistence.
